#ifndef __BENCHMARK
#define __BENCHMARK

#include "mss.h"

enum BENCHMARK {
	BENCHMARK_PREPARE,
	BENCHMARK_MSS_KEYGEN,
	BENCHMARK_MSS_SIGN,
	BENCHMARK_MSS_PREPARE_VERIFY,
	BENCHMARK_MSS_VERIFY,
	BENCHMARK_WINTERNITZ_KEYGEN,
	BENCHMARK_WINTERNITZ_SIGN,
	BENCHMARK_WINTERNITZ_VERIFY,
	BENCHMARK_HASH_CALC,
	BENCHMARK_AES_CALC
};

void do_benchmark(enum BENCHMARK phase, unsigned short benchs);

#endif // __BENCHMARK
